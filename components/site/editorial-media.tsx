import Link from 'next/link';
import {photos} from '@/data/redesign';
export function CampusPhoto({asset='gate',className='',priority=false}:{asset?:keyof typeof photos;className?:string;priority?:boolean}){const p=photos[asset];return <figure className={'editorial-photo '+className}><div className="photo-crop"><img src={p.src} alt={p.alt} width={p.width} height={p.height} loading={priority?'eager':'lazy'} fetchPriority={priority?'high':'auto'} decoding="async"/></div><figcaption><span>{p.title}</span><Link href="/credits">Archive · {p.date} · Photo credit ↗</Link></figcaption></figure>}
export function EventPhoto({title,wide=false}:{title:string;wide?:boolean}){return <div className={'event-photo-slot '+(wide?'wide':'')}><div className="event-slot-top"><span>GNDU / EVENT ARCHIVE</span><span>PHOTOGRAPH PENDING</span></div><div><span className="event-slot-title">{title}</span><p>Approved event photography will appear here.</p></div><div className="event-slot-bottom">A record of building, learning and collaboration.<span aria-hidden="true">↗</span></div></div>}
